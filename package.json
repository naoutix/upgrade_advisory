{
  "name": "pledge-fair-upgrade-advisor",
  "version": "1.0.0",
  "private": true,
  "description": "Génère docs/data.json pour l'app statique Pledge Fair (GitHub Pages).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "update-data": "node scripts/update-data.mjs",
    "test": "node --test",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "prettier": "^3.9.5"
  }
}
